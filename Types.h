#ifndef TYPES_H
#define TYPES_H

// ============================================================================
// Types.h — shared plain data types used across the VirkMotion project:
// a minimal 3D vector, the queued-move representation used by CommandQueue,
// and the runtime state of the cartesian move currently in progress
// (used by CartesianInterpolation).
// ============================================================================

#include "Kinematics.h"
#include "TrapProfile.h"

#endif
